// author: kodeholic (powered by Claude)

# 작업지침 — 문서 현행화 2건 (요구문서 §F-2 정정 + PROJECT_MASTER claudecode 경로)

> **작성**: 김대리 (claude.ai) / **수행**: 김과장 (Claude Code) / **결정**: 부장님 (kodeholic — 명시 지시 "2개 다 지침에 넣어")
> **성격**: 문서 현행화 전용. **코드 변경 0.** 정확한 텍스트 교체(str_replace 단위).
> **근거**: 김과장 조사 보고 `20260626a_..._done.md` 가 "5크레이트 전체 297개" 사실 확인 → 요구문서 §F-2 약점3("1층 거의 빔")이 oxe2e 한정으로만 참 → 정정. + PROJECT_MASTER 의 claudecode/ 경로가 실제(context/YYYYMM/ 통합)와 어긋남.

> ⚠ **PROJECT_MASTER 수정은 평소 금지(명시 지시 필요).** 이번은 부장님 "2개 다 지침에 넣어" 명시 지시 → 정당. 이 지침 범위 밖 마스터 수정 금지.

---

## §0 의무 점검

1. **문서 2개만 수정. 코드·테스트 일절 손대지 않는다.**
2. 교체는 **아래 명시된 정확한 텍스트**로만. 임의 의역/추가/삭제 금지.
3. str_replace 가 unique match 안 되면(텍스트 변형됐으면) 중단 + 보고. 추측 교체 금지.

---

## §1 작업 1 — 요구문서 §F-2 약점3 정정

**파일**: `context/202606/20260626_oxe2e_regression_requirements.md`

### 1-A. §F-2 약점 3 교체

**찾을 텍스트** (정확히):
```
- **약점 3 — 1층(유닛) 거의 빔**: oxe2e 전체 `#[cfg(test)]` = judge caching 2개뿐. §23 5영역(rewrite/식별/자료구조 정합/scope) 유닛 0. (REQ-1.1 의 근거. "Rust 관례 테스트 확장"의 빈자리.)
```

**바꿀 텍스트**:
```
- **약점 3 — 1층(유닛)은 oxe2e 한정으로만 빈다 (★0626a 조사로 정정)**: 처음 "1층 거의 빔"이라 적었으나, 그건 oxe2e 크레이트만 본 과일반화였다. **5개 크레이트 전체로는 유닛 테스트 297개가 이미 존재**(grep 검증, 0626a). ⑤rewrite·③자료구조정합·②상태전이·④scope/라우팅은 **두텁고**, **①식별/통지가 상대적으로 얇다.** `tests/` 통합 디렉토리는 없음(전부 파일 내 `#[cfg(test)] mod tests`). oxe2e 크레이트 자체는 judge caching 2개뿐인 게 맞다. → **작업이 "1층 신설"이 아니라 "①식별/통지 보강 + 2층 정밀화"로 좁아진다**(부장님 저비용 축에 유리). REQ-1.1 은 "5영역 전부 신설"이 아니라 "①보강 + 나머지는 빈칸 점검"으로 읽을 것.
```

### 1-B. §A-2 하단 경고 박스 보강 (선택 — 같이 정정)

**찾을 텍스트**:
```
> ⚠ **현행 실측으로 이 진단이 일부 수정됨 → §F 참조.** A-2 는 세션 1 시점의 추론. 실제 코드엔 음성 단언·
> 카운터 교차가 이미 존재. 진짜 약점은 "단언 부재"가 아니라 "있는 단언이 binary(count==0)라 안 떨어짐".
```

**바꿀 텍스트**:
```
> ⚠ **현행 실측으로 이 진단이 일부 수정됨 → §F 참조.** A-2 는 세션 1 시점의 추론. 실제 코드엔 음성 단언·
> 카운터 교차가 이미 존재(§F-1). 1층 유닛도 5크레이트 297개로 두텁다(§F-2, 0626a 조사 — ①식별/통지만 얇음).
> 진짜 약점은 "단언 부재"가 아니라 "있는 판정이 binary(count==0)라 안 떨어짐 + 봇이 seq 를 버려 등식 원료 없음".
```

### 1-C. §I 한 줄 결론 정정

**찾을 텍스트**:
```
> **현행 oxe2e 의 진짜 약점은 "단언 부재"가 아니라 "있는 단언이 binary(count==0)라 안 떨어짐 + 봇이 seq 를
> 버려 등식의 원료가 없음 + 1층이 비어 구조 검증을 2층에 떠넘김"** — 이 셋의 정밀화가 첫 실물 작업.
```

**바꿀 텍스트**:
```
> **현행 oxe2e 의 진짜 약점은 "단언 부재"가 아니라 "있는 판정이 binary(count==0)라 안 떨어짐 + 봇이 seq 를
> 버려 등식의 원료가 없음"** (1층 유닛은 297개로 두텁고 ①식별/통지만 얇다 — 0626a). 첫 실물 작업 = 봇 seq
> 원료 적재(2층 정밀화 ①) → count→등식 판정 → build_tracks_update 추출 + 수렴 단언.
```

---

## §2 작업 2 — PROJECT_MASTER claudecode 경로 현행화

**파일**: `context/PROJECT_MASTER.md` (★ `/mnt/project/` 아님 — 실제 맥북 파일. `/Users/tgkang/repository/context/PROJECT_MASTER.md`)

> 실제 작업지침 자리가 `context/claudecode/YYYYMM/` → `context/YYYYMM/` 로 통합됐는데 마스터가 안 따라옴.
> 3자리 정정. **의미: claudecode/ 디렉토리 폐기, 지침·완료보고 모두 context/YYYYMM/ 단일 자리.**

### 2-A. "작업 지침 파일 자리" 절

**찾을 텍스트**:
```
- **작업 지침**: `~/repository/context/claudecode/YYYYMM/YYYYMMDD<suffix>_<topic>.md` — 단일 출처
- **작업 완료 보고**: `~/repository/context/YYYYMM/YYYYMMDD<suffix>_<topic>_done.md`
- **두 자리 혼동 금지** — 김과장이 완료 보고를 claudecode/ 자리에 만들었던 사고 1회 (0517c)
```

**바꿀 텍스트**:
```
- **작업 지침**: `~/repository/context/YYYYMM/YYYYMMDD<suffix>_<topic>.md` — 단일 출처 (구 `claudecode/` 폐기, context/YYYYMM/ 통합, 2026-06-26)
- **작업 완료 보고**: `~/repository/context/YYYYMM/YYYYMMDD<suffix>_<topic>_done.md`
- **지침과 완료보고 = 같은 디렉토리(context/YYYYMM/), suffix `_done` 으로 구분** — 구 두-자리 혼동 사고(0517c)는 자리 통합으로 소멸
```

### 2-B. 디렉토리 구조 다이어그램

**찾을 텍스트**:
```
├── claudecode/      ← 김대리 작업 지침 파일 (YYYYMM/YYYYMMDD<suffix>_<topic>.md)
```

**바꿀 텍스트** (이 줄을 **삭제** — claudecode/ 디렉토리 폐기. 즉 위 한 줄을 빈 줄 없이 제거):
```
```
(= 해당 줄 통째 삭제. 단 다이어그램 정렬 깨지지 않게 그 줄만 제거하고 앞뒤 줄은 유지.)

> 보강: 같은 다이어그램에서 `202603/ 202604/ 202605/` 만 있고 `202606/` 가 없으면 `202605/` 아래에 `│   202606/ ...` 줄 추가 — **단 현재 텍스트에 202606 이 이미 있으면 건드리지 말 것.** (확인 후 판단, 없을 때만 추가.)

---

## §3 변경 영향 범위

- `context/202606/20260626_oxe2e_regression_requirements.md` — §A-2 / §F-2 / §I 3자리.
- `context/PROJECT_MASTER.md` — 작업지침자리 절 + 디렉토리 다이어그램 2자리.
- **코드 0. 테스트 0.**

---

## §4 운영 룰

1. **명시된 정확한 텍스트로만 교체.** unique match 실패 시 중단+보고(추측 금지).
2. **마스터는 이 2자리만.** 다른 claudecode 언급 발견해도 손대지 말고 "발견_사항" 보고만(이 지침 범위 밖).
3. 교체 후 두 파일 다시 읽어 의도대로 됐는지 self-verify.

---

## §5 산출물

- 수정된 문서 2개.
- **완료 보고**: `context/202606/20260626c_doc_resync_done.md` — 교체 5자리 확인 + "발견_사항"(마스터에 또 다른 claudecode 언급 있었나).
- SESSION_INDEX_202606.md 한 줄.

---

## §6 기각

- 코드/테스트 수정.
- 마스터의 이 2자리 밖 수정.
- 텍스트 의역(정확 교체만).

---

## §7 시작 전 확인

- [ ] 대상 파일 2개 경로 확인(요구문서 = context/202606/, 마스터 = context/PROJECT_MASTER.md, /mnt/ 아님).
- [ ] 찾을 텍스트 5개가 각 파일에 unique 한지 grep 선확인.
- [ ] 202606 다이어그램 줄 이미 있는지 확인(있으면 2-B 보강 skip).

---

*author: kodeholic (powered by Claude)*
*문서 현행화 2건. 코드 0. 부장님 명시 지시로 마스터 수정 정당화.*
