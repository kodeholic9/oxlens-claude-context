┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  App      │     │  Engine   │     │  Hub(WS)  │     │  sfud     │
│(demo/app) │     │(SDK Core) │     │  Gateway  │     │(SFU Media)│
└────┬─────┘     └────┬─────┘     └────┬─────┘     └────┬─────┘
     │                │                │                │
     │ new Engine()   │                │                │
     │───────────────>│                │                │
     │                │                │                │
     │ sdk.connect()  │                │                │
     │───────────────>│                │                │
     │                │  WS Connect    │                │
     │                │───────────────>│                │
     │                │                │                │
     │                │  WS Open       │                │
     │                │<───────────────│                │
     │                │                │                │
     │                │  IDENTIFY(3)   │                │
     │                │  token=...     │                │
     │                │───────────────>│  JWT verify    │
     │                │                │───────────────>│
     │                │                │                │
     │                │  HELLO(0)      │                │
     │                │  heartbeat_int │                │
     │                │<───────────────│                │
     │                │                │                │
     │  conn:state    │                │                │
     │  =IDENTIFIED   │                │                │
     │<───────────────│                │                │
     │                │                │                │
     │                │ Phase: IDLE → CONNECTED         │
     │                │                │                │
     ╠════════════════╬═══ joinRoom() ═╬════════════════╣
     │                │                │                │
     │ await joinRoom │                │                │
     │───────────────>│                │                │
     │                │ ROOM_JOIN(11)  │                │
     │                │───────────────>│  gRPC Handle   │
     │                │                │───────────────>│
     │                │                │                │ Room lookup
     │                │                │                │ Participant 등록
     │                │                │ JOIN OK        │
     │                │  JOIN OK       │<───────────────│
     │                │  server_config │                │
     │                │  participants  │                │
     │                │  tracks[]      │                │
     │                │<───────────────│                │
     │                │                │                │
     │                │ ┌─────────────────────────────┐ │
     │                │ │ _onJoinOk() 내부:            │ │
     │                │ │                             │ │
     │                │ │ 1. Room 생성 + hydrate()     │ │
     │                │ │    - localEndpoint 생성      │ │
     │                │ │    - mic Pipe (send, active) │ │
     │                │ │    - cam Pipe (send, active) │ │
     │                │ │    - remote Pipe[]           │ │
     │                │ │    - FloorFsm 생성           │ │
     │                │ │                             │ │
     │                │ │ Phase: CONNECTED → JOINED    │ │
     │                │ │                             │ │
     │                │ │ 2. setupPublishPc()          │ │
     │                │ │    pubPc = new RTCPeerConn   │ │
     │                │ │                             │ │
     │                │ │    addTransceiver("audio")   │ │
     │                │ │    ← track 없이 kind 문자열  │ │
     │                │ │    micPipe.bindSender()      │ │
     │                │ │                             │ │
     │                │ │    cameraPipe → DEFERRED     │ │
     │                │ │    ← videoTrack 없으므로     │ │
     │                │ │      transceiver 미생성      │ │
     │                │ │                             │ │
     │                │ │    createDataChannel         │ │
     │                │ │    ("unreliable")            │ │
     │                │ │                             │ │
     │                │ │    createOffer()             │ │
     │                │ │    setLocalDescription()     │ │
     │                │ │    buildPublishRemoteAnswer() │ │
     │                │ │    setRemoteDescription()    │ │
     │                │ │                             │ │
     │                │ │ 3. emit room:joined          │ │
     │                │ │                             │ │
     │                │ │ 4. queueSubscribeReNego()    │ │
     │                │ │    subPc = new RTCPeerConn   │ │
     │                │ │    buildSubscribeRemoteSdp() │ │
     │                │ │    setRemoteDescription()    │ │
     │                │ │    createAnswer()            │ │
     │                │ │    setLocalDescription()     │ │
     │                │ │                             │ │
     │                │ │ 5. joinRoom Promise resolve  │ │
     │                │ └─────────────────────────────┘ │
     │                │                │                │
     │ joinRoom 완료  │                │                │
     │ (33ms, 미디어 0)│                │                │
     │<───────────────│                │                │
     │                │                │                │
     ╠════════════════╬═══ enableMic() ╬════════════════╣
     │                │                │                │
     │ await enableMic│                │                │
     │───────────────>│                │                │
     │                │                │                │
     │                │ acquire.audio()│                │
     │                │ getUserMedia   │                │
     │                │ ({audio:true}) │  ← ~10ms       │
     │                │                │                │
     │                │ pipe.setTrack  │                │
     │                │ (audioTrack)   │                │
     │                │ ← 기존 sender에│                │
     │                │   replaceTrack │                │
     │                │                │                │
     │                │ PUBLISH_TRACKS │                │
     │                │ (15, audio,    │                │
     │                │  duplex=full)  │  gRPC Handle   │
     │                │───────────────>│───────────────>│
     │                │                │                │ MediaIntent 등록
     │                │                │                │ (RTP 대기)
     │                │ Phase: JOINED → PUBLISHING      │
     │                │                │                │
     ╠════════════════╬═ enableCamera()╬════════════════╣
     │                │                │                │
     │ await          │                │                │
     │ enableCamera   │                │                │
     │───────────────>│                │                │
     │                │                │                │
     │                │ acquire.video()│                │
     │                │ getUserMedia   │                │
     │                │ ({video:       │                │
     │                │  1280x720@24}) │  ← ~200ms      │
     │                │                │                │
     │                │ ★★★ 핵심 구간 ★★★              │
     │                │ pubPc.addTrans │                │
     │                │ ceiver(video   │                │
     │                │ Track,sendonly)│                │
     │                │                │                │
     │                │ ┌─────────────────────────────┐ │
     │                │ │ Chrome 내부:                 │ │
     │                │ │                             │ │
     │                │ │ OpenH264 인코더 할당 시도    │ │
     │                │ │                             │ │
     │                │ │ ✅ 정상: encoder=OpenH264    │ │
     │                │ │    framesEncoded 즉시 증가   │ │
     │                │ │                             │ │
     │                │ │ ❌ encoder=null 케이스:      │ │
     │                │ │    다른 탭(video_radio)이    │ │
     │                │ │    같은 카메라를 점유 중     │ │
     │                │ │    + hot_standby 상태       │ │
     │                │ │    (인코더 해제 + 카메라     │ │
     │                │ │     capture 유지)            │ │
     │                │ │    → 인코더 리소스 경합      │ │
     │                │ │    → 할당 실패               │ │
     │                │ │    → framesEncoded = 0       │ │
     │                │ │    → RTP 패킷 0개            │ │
     │                │ └─────────────────────────────┘ │
     │                │                │                │
     │                │ reNegoPublish()│                │
     │                │ createOffer    │                │
     │                │ setLocal       │                │
     │                │ buildAnswer    │                │
     │                │ setRemote      │                │
     │                │                │                │
     │                │ PUBLISH_TRACKS │                │
     │                │ (15, video,    │  gRPC Handle   │
     │                │  duplex=full)  │                │
     │                │───────────────>│───────────────>│
     │                │                │                │ MediaIntent 등록
     │                │                │                │
     ╠════════════════╬═══ ICE/DTLS ═══╬════════════════╣
     │                │                │                │
     │                │  pubPc ICE     │                │
     │                │  connected     │                │
     │                │  (STUN B/R)    │                │
     │                │ ·· ·· ·· ·· ··>│UDP 단일포트    │
     │                │                │· · · · · · · ·>│
     │                │                │                │
     │                │  DTLS handshake│                │
     │                │  (pubPc)       │                │
     │                │ ════════════════════════════════>│
     │                │                │                │
     │                │  pubPc.conn    │                │
     │                │  State=        │                │
     │                │  'connected'   │                │
     │                │                │                │
     │                │ Phase: PUBLISHING → READY       │
     │                │                │                │
     ╠════════════════╬═ 미디어 흐름 ══╬════════════════╣
     │                │                │                │
     │                │  ── RTP Audio (Opus) ──────────>│
     │                │                │                │ stream_map 등록
     │                │                │                │ SSRC ↔ audio
     │                │                │                │
     │                │  ── RTP Video (H264/VP8) ─────>│ (encoder=null이면
     │                │                │                │  이 패킷이 0개)
     │                │                │                │ stream_map 등록
     │                │                │                │ SSRC ↔ video
     │                │                │                │
     │                │                │  TRACKS_UPDATE │
     │                │  TRACKS_UPDATE │  (101, 발견된  │
     │                │  (101, add)    │   트랙 broadcast)
     │                │<───────────────│<───────────────│
     │                │                │                │
     │                │  ── SCTP/DCEP (DataChannel) ──>│
     │                │                │                │ dc_unreliable_ready
     │                │  DC Open       │                │ pending_buf drain
     │                │                │                │
     ╠════════════════╬═══ 상호 통신 ══╬════════════════╣
     │                │                │                │
     │                │  ◄── RTP (상대방 audio) ───────│ fan-out
     │                │  ◄── RTP (상대방 video) ───────│
     │                │                │                │
     │                │  subPc ontrack │                │
     │                │  → media:track │                │
     │  media:track   │  이벤트        │                │
     │<───────────────│                │                │
     │  비디오 렌더링  │                │                │
     │                │                │                │
     │         ★ 상호 미디어 통신 가능 ★                │
     └────────────────┴────────────────┴────────────────┘