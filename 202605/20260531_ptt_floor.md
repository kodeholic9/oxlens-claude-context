# 4. PTT & Floor

> 사람이 읽는 문서. 그림 먼저, 왜를 남긴다. → 지도는 `20260531_sfu_overview.md`. 자료구조 토대는 `20260531_state_ownership.md`(2장).
> **[골격]** 목차만 잡힘. 본문은 세션별로 한 절씩.

half-duplex(무전) 가 어떻게 한 슬롯을 공유하나. PTT 는 OxLens 의 핵심 차별점.

---

## 4.1 트랙 속성 — duplex × simulcast (RoomMode 없음)
- [본문 TODO] `track = duplex(full/half) + simulcast(on/off) + priority`. **방은 빈 그릇**(room.mode 제거). full+half 공존 가능. 파생 규칙: half → simulcast 강제 off(PttRewriter + SimulcastRewriter 충돌).
- [다이어그램 자리]

## 4.2 slot — 방 공유 단일 SSRC
- [본문 TODO] Half 트랙은 개인 stream 이 아니라 방 slot 1개를 공유. per-room virtual_ssrc. `ptt-{room}-audio/video` track_id. 왜 슬롯인가(N명 발화자 → 수신자는 1개 m-line).
- [다이어그램 자리]

## 4.3 PttRewriter — SSRC/seq/ts 오프셋
- [본문 TODO] 화자 전환 시 가상 SSRC 로 rewrite, ts 오프셋. dynamic ts_gap. silence flush(NetEQ 연속성). VP8 키프레임 대기. clear_speaker 멱등.

## 4.4 Floor — 발화권
- [본문 TODO] FloorController. floor 판정은 **방별 독립**(집계 레이어 없음). prefan 에서 current_speaker 검사. PLI burst(Granted 시).

## 4.5 MBCP — DC-only Floor (3GPP TS 24.380)
- [본문 TODO] native TLV 포맷. unreliable DC 채널. T101/T104(클라)/T132(서버) 재전송. WS Floor path 완전 삭제(bearer=ws 도 동일 바이너리를 WS binary 로). viaRoom/destinations TLV.
- [다이어그램 자리] bearer 왕복 (클라→hub→sfud→floor→hub→클라).

## 4.6 Freeze Masking (클라)
- [본문 TODO] 숨김=`left:-9999px`+`overflow:hidden`(즉각, 시그널링 기반). 표시=listening AND rVFC. **`display:none` 금지**(디코더 정지 → onmute 연쇄 장애).

## 4.7 Duplex 전환 — full↔half 캐싱 (2026-05-31) ★
- [본문 TODO] `TRACK_STATE_REQ`(0x1106) **단일 경로**(PUBLISH_TRACKS hot-swap 분리, 이중화 금지). full→half: 개인 stream 보존(republish 폐기) + active:false 통지. half→full: 보존 Weak 자동 재송출 + active:true / 신규 sub add. 생명주기 기준(collect active:false → release_stale_mids 회수 제외). 분류 권위는 TrackType(3장 3.2). **클라 UI 패러다임 전환(개인grid↔PTT slot)은 별도 — 미착수.**
- [다이어그램 자리] full↔half 상태 + 통지 분기.

---

## 불변식
> [TODO] half → simulcast 강제 off / PTT 원본 SSRC 는 subscriber 에 relay 안 됨(가상만) / PTT audio track.enabled 항상 true(서버 floor gating) / virtual track remove 는 잔존자 체크 / display:none 금지 / clear_speaker 멱등.

## 기각한 대안
> [TODO] FloorService 물리 분리(레이턴시 재앙) / WS Floor fallback 유지(DC-only 정답) / FLOOR_PING(RTP liveness 정답) / op=52 SWITCH_DUPLEX(→ TRACK_STATE_REQ) / full→half republish(→ 캐싱 보존) / Pan-Floor(N방 동시 발화) 등.

---

*author: kodeholic (powered by Claude)*
